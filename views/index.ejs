<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | EMV Bazar</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;600;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-orange: #f97316;
            --primary-green: #22c55e;
            --bg-white: #ffffff;
            --text-dark: #1e293b;
            --text-muted: #64748b;
            --border: #e2e8f0;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Outfit', sans-serif;
        }

        body {
            background-color: #f8fafc;
            color: var(--text-dark);
            min-height: 100vh;
        }

        main {
            padding: 3rem 2rem;
            max-width: 1000px;
            margin: 0 auto;
        }

        h2 {
            margin-bottom: 2rem;
            font-size: 1.8rem;
            font-weight: 800;
            text-align: center;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1.5rem;
        }

        .box {
            background: white;
            padding: 1.25rem 1.5rem;
            border-radius: 16px;
            border: 1px solid var(--border);
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: transform 0.2s, box-shadow 0.2s;
            cursor: pointer;
        }

        .box:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        }

        .box.monthly {
            border-left: 8px solid var(--primary-orange);
        }

        .box.fixed {
            border-left: 8px solid var(--primary-green);
        }

        .box.uncertain {
            border-left: 8px solid #64748b;
        }

        .box h2 {
            font-size: 2rem;
            /* color: var(--text-muted); */
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            margin-bottom: 0;
        }

        .box .count {
            font-size: 2rem;
            font-weight: 800;
            color: var(--text-dark);
        }

        @media (max-width: 768px) {
            .grid {
                grid-template-columns: 1fr;
                gap: 1rem;
            }

            .box {
                padding: 1rem 1.25rem;
            }

            .box .count {
                font-size: 1.5rem;
            }
        }
    </style>
</head>

<body>
    <%- include('partials/navbar') %>

        <main>
            <h2>Debt Tracker</h2>

            <div class="grid">
                <a href="/users/monthly" style="text-decoration: none; color: inherit;">
                    <div class="box monthly">
                        <h2>Monthly</h2>
                        <div class="count">
                            <%= monthlyCount %>
                        </div>
                    </div>
                </a>

                <a href="/users/fixed" style="text-decoration: none; color: inherit;">
                    <div class="box fixed">
                        <h2>Fixed</h2>
                        <div class="count">
                            <%= fixedCount %>
                        </div>
                    </div>
                </a>

                <a href="/users/uncertain" style="text-decoration: none; color: inherit;">
                    <div class="box uncertain">
                        <h2>Uncertain</h2>
                        <div class="count">
                            <%= uncertainCount %>
                        </div>
                    </div>
                </a>
            </div>
        </main>
</body>

</html>